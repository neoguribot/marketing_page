# 소니코리아 검색광고 마케팅 대시보드

네이버 검색광고 리포트(소니코리아 쇼핑몰 캠페인)를 분석하고 Streamlit 대시보드로
시각화하는 프로젝트. 이 문서는 향후 Claude Code 세션이 참고할 작업 규칙과 데이터
맥락을 담는다.

## 프로젝트 개요

- **광고주**: 소니 (업종: 쇼핑몰·리테일)
- **캠페인**: 소니코리아, 목표 = 판매, 입찰 전략 = 전환수 최대화
- **데이터 기간**: 2026-07-06 ~ 2026-08-04 (30일)
- **플랫폼**: 네이버 검색광고 (계정 123-456-7890, 실습 계정)
- **핵심 이슈**(diagnosis.csv 참고): 일 예산이 수요를 못 따라감(노출 손실 57.5%),
  광고 소재 부족, 키워드가 광고그룹 1개에 몰려있음(품질평가점수 5.6로 낮음), 광고
  확장 미사용, 학습 기간(7일) 영향

## 환경 설정

```bash
source venv/bin/activate       # Python 3.14 (homebrew), 가상환경은 이미 구성됨
pip install -r requirements.txt
streamlit run app.py           # http://localhost:8501
```

새 패키지를 추가하면 반드시 `pip freeze | grep -Ei "pandas|numpy|streamlit|plotly|openpyxl"`
등으로 `requirements.txt`를 갱신할 것 (전체 freeze 금지 — venv 내부 불필요한 패키지까지
고정되는 것을 방지).

## 데이터 구조 (`raw_data/*.csv`)

모든 CSV는 UTF-8 BOM(`utf-8-sig`)으로 인코딩되어 있음 — `encoding="utf-8-sig"` 없이
읽으면 첫 컬럼명이 깨진다.

| 파일 | 내용 | 그레인 |
|---|---|---|
| `campaign_daily.csv` | 일별 성과 (노출/클릭/CTR/CPC/비용/구매/전환율) | 일자 |
| `keywords.csv` | 키워드별 성과 + 품질평가점수 | 키워드 |
| `search_terms.csv` | 실제 검색어(사용자가 입력한 쿼리) | 검색어 |
| `ad_groups.csv` | 광고그룹 요약 (현재 광고그룹 1개뿐) | 광고그룹 |
| `device_hour.csv` | 기기별 + 시간대별 성과가 한 파일에 세로로 결합됨 (`구분` 컬럼으로 구분: `기기` / `시간대`) | 기기 or 시간대 |
| `placements.csv` | 게재위치 리포트 — **현재 데이터 없음(빈 파일)**. 코드에서 항상 empty-case 처리할 것 | 게재위치 |
| `diagnosis.csv` | 두 블록 혼재: 상단 6줄은 `key,value` 계정 메타정보, 빈 줄 이후는 진단 이슈 표(심각도/문제/설명/근거/조치) | 계정 메타 + 이슈 |

### 알아둬야 할 데이터 함정

- `campaign_daily.csv` 마지막 행은 `날짜` 컬럼 값이 `"합계"`인 **총계 행**이다.
  날짜 파싱 전에 반드시 제외한다 (`data_loader.py`의 `load_campaign_daily` 참고).
- CTR·전환율 등은 `"7.58%"` 같은 문자열이다. 숫자로 쓸 때는 `%` 제거 후 float
  변환(`_pct_to_float`), 값은 **퍼센트 단위(0~100)** 로 유지한다. 0~1 정규화와
  섞어 쓰지 말 것.
- 금액(비용, CPC, CPA)은 원화 정수, 천 단위 구분 없이 저장되어 있음.
- **매출/전환가치 데이터가 없다** — `구매`는 건수(count)만 있고 금액이 없으므로
  ROAS·매출 지표는 계산 불가. CPA(전환당비용)와 전환율까지가 이 데이터로 낼 수
  있는 한계다. 이 한계를 넘어서는 지표를 요청받으면 데이터 부재를 먼저 알릴 것.
- `keywords.csv`에는 완전 동일한 키워드가 중복 행으로 나타날 수 있다(예: "소니스토어
  매장 찾기"가 두 번, 성과가 다름 — 매칭타입/기간 세분화 차이로 추정). 집계 시
  `groupby(키워드)`로 합산할지, 원본 그대로 보여줄지 분석 목적에 맞게 판단할 것.
- `pandas 3.x` + `nullable dtype` 조합에서 `Series.replace(0, pd.NA).round()`가
  `TypeError`를 낸다. 결측 대체는 `float("nan")`을 쓴다.

## 코드 구조

- `data_loader.py` — CSV 로딩·정제 전담. **여기서 정제된 데이터만** `app.py`와
  향후 분석 스크립트/노트북이 사용한다. CSV의 원시 텍스트(퍼센트 문자열, BOM,
  합계행 등)를 다른 곳에서 다시 파싱하지 않는다.
- `app.py` — Streamlit 대시보드. 섹션 순서: KPI → 계정 진단 이슈 → 일별 추이 →
  기기/시간대 → 키워드/검색어/광고그룹.
- 새 분석(주피터 노트북, 스크립트 등)을 추가할 때도 `data_loader.load_all()`을
  통해 데이터를 가져온다.

## 시각화 규칙

- 색상은 `app.py` 상단에 정의된 팔레트(BLUE/ORANGE/AQUA/YELLOW/MAGENTA/GREEN/
  VIOLET/RED, 이 순서 고정)를 그대로 쓴다. 임의로 순서를 바꾸거나 새 hex를
  추가하지 말 것 — 색맹 접근성 검증을 거친 고정 순서다.
- **이중 축(dual-axis) 차트 금지.** 스케일이 다른 두 지표를 한 차트에 넣지
  않는다 — 별도 차트 두 개 또는 지표를 정규화해서 비교한다.
- 심각도/상태 표시(진단 이슈의 높음·보통·낮음 등)는 STATUS 팔레트
  (good/warning/serious/critical)를 쓰고, 색만으로 의미를 전달하지 않도록
  라벨(뱃지 텍스트)을 항상 같이 붙인다.
- 카테고리 3개 이하일 때만 파이/도넛 차트 사용. 4개 이상이면 막대 차트로 전환.

## 분석 시 우선순위

1. **예산 소진 문제**가 가장 심각한 이슈(높음)다 — 시간대별 성과 분석 시 이른
   시간에 예산이 소진되어 저녁 시간대 기회손실이 있는지 항상 함께 짚어준다.
2. 광고그룹이 1개뿐이라 세그먼트 분석(광고그룹 간 비교)은 현재 의미가 없다.
   키워드 주제별(헤드폰/카메라/오디오/플레이스테이션 등) 그룹핑을 대신 활용할 것.
3. 학습 기간(7일, `학습 기간=Y`) 동안의 성과는 정상보다 낮게 나오는 것이 기대되는
   구간이므로, 추세 분석 시 학습 기간 포함/제외 두 버전을 함께 보여주는 것을
   권장한다.

## 하지 말 것

- `venv/`, `raw_data/*.csv`를 git에 커밋하기 전에 사용자에게 먼저 확인 (원본
  리포트 데이터는 민감할 수 있음, 현재 이 디렉토리는 git 저장소 아님).
- 없는 지표(매출, ROAS, LTV 등)를 추정치로 지어내지 않기 — 데이터에 없으면
  없다고 말한다.
- `requirements.txt`를 전체 `pip freeze`로 덮어쓰지 않기 (venv 오염 방지).
